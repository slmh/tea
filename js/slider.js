'use strict';

// Slider reviews
$(document).ready(function(){
   $('.why__reviews').slick({
      slidesToShow: 1,
      infinite: true,
      autoplay: true,
      autoplaySpeed: 5000
   });
});
// *** Slider reviews END